---
phase: 01-foundation-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - site-app/package.json
  - site-app/tsconfig.json
  - site-app/next.config.ts
  - site-app/tailwind.config.ts
  - site-app/postcss.config.mjs
  - site-app/src/app/globals.css
  - site-app/src/app/layout.tsx
  - site-app/src/app/page.tsx
  - site-app/src/lib/fonts.ts
  - site-app/Dockerfile
  - admin-app/package.json
  - admin-app/tsconfig.json
  - admin-app/next.config.ts
  - admin-app/tailwind.config.ts
  - admin-app/postcss.config.mjs
  - admin-app/src/app/globals.css
  - admin-app/src/app/layout.tsx
  - admin-app/src/app/page.tsx
  - admin-app/src/lib/fonts.ts
  - admin-app/Dockerfile
autonomous: true
requirements:
  - RTL-01
  - RTL-02
  - RTL-04

must_haves:
  truths:
    - "site-app root html tag has dir='rtl' and lang='fa'"
    - "admin-app root html tag has dir='rtl' and lang='fa'"
    - "Vazirmatn font loads via next/font with weights 300, 400, 500, 700 and zero layout shift"
    - "Tailwind CSS configured with logical properties plugin for RTL support"
    - "Both apps build and start without errors"
  artifacts:
    - path: "site-app/src/app/layout.tsx"
      provides: "Root layout with RTL direction and Vazirmatn font"
      contains: "dir=\"rtl\""
    - path: "site-app/src/lib/fonts.ts"
      provides: "Vazirmatn font configuration via next/font"
      contains: "Vazirmatn"
    - path: "admin-app/src/app/layout.tsx"
      provides: "Admin root layout with RTL direction and Vazirmatn font"
      contains: "dir=\"rtl\""
    - path: "site-app/Dockerfile"
      provides: "Multi-stage Next.js Docker build"
      contains: "next build"
    - path: "admin-app/Dockerfile"
      provides: "Admin Next.js Docker build"
      contains: "next build"
  key_links:
    - from: "site-app/src/app/layout.tsx"
      to: "site-app/src/lib/fonts.ts"
      via: "font import and className application"
      pattern: "vazirmatn\\.className"
    - from: "admin-app/src/app/layout.tsx"
      to: "admin-app/src/lib/fonts.ts"
      via: "font import and className application"
      pattern: "vazirmatn\\.className"
    - from: "site-app/src/app/layout.tsx"
      to: "site-app/src/app/globals.css"
      via: "global CSS import"
      pattern: "import.*globals.css"
---

<objective>
Scaffold both Next.js 15 applications (site-app and admin-app) with RTL root layout, Vazirmatn Persian font via next/font, and Tailwind CSS configured for logical properties.

Purpose: Establishes the frontend foundation with RTL baked in from day one. Per STATE.md decision, RTL must be wired in Phase 1 — retrofitting is painful and risky.
Output: Two Next.js apps that build and render with Persian RTL direction, Vazirmatn font, and Tailwind logical property support.
</objective>

<execution_context>
@/Users/hadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold site-app with Next.js 15, RTL layout, Vazirmatn font, and Tailwind CSS</name>
  <files>site-app/package.json, site-app/tsconfig.json, site-app/next.config.ts, site-app/tailwind.config.ts, site-app/postcss.config.mjs, site-app/src/app/globals.css, site-app/src/app/layout.tsx, site-app/src/app/page.tsx, site-app/src/lib/fonts.ts, site-app/Dockerfile</files>
  <action>
Create `site-app/` using Next.js 15 with App Router:
```bash
cd /Users/hadi/Documents/Self/kidtube && npx create-next-app@latest site-app --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm
```

After scaffold, make these modifications:

**1. Install Vazirmatn font package:**
```bash
cd site-app && npm install vazirmatn
```
Note: The `vazirmatn` npm package provides the font files. We use `next/font/local` to load them for optimal performance and zero layout shift.

**2. Create `site-app/src/lib/fonts.ts`:**
```typescript
import localFont from 'next/font/local';

export const vazirmatn = localFont({
  src: [
    {
      path: '../../node_modules/vazirmatn/fonts/webfonts/Vazirmatn-Light.woff2',
      weight: '300',
      style: 'normal',
    },
    {
      path: '../../node_modules/vazirmatn/fonts/webfonts/Vazirmatn-Regular.woff2',
      weight: '400',
      style: 'normal',
    },
    {
      path: '../../node_modules/vazirmatn/fonts/webfonts/Vazirmatn-Medium.woff2',
      weight: '500',
      style: 'normal',
    },
    {
      path: '../../node_modules/vazirmatn/fonts/webfonts/Vazirmatn-Bold.woff2',
      weight: '700',
      style: 'normal',
    },
  ],
  display: 'swap',
  variable: '--font-vazirmatn',
});
```

If the woff2 paths from the npm package don't resolve correctly, fall back to `next/font/google` with `Vazirmatn` — but `next/font/local` from the npm package is preferred for offline/Docker builds.

**3. Update `site-app/src/app/layout.tsx`:**
```tsx
import type { Metadata } from 'next';
import { vazirmatn } from '@/lib/fonts';
import './globals.css';

export const metadata: Metadata = {
  title: 'کیدتیوب',
  description: 'ویدیوهای آموزشی فارسی برای کودکان',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="fa" dir="rtl" className={vazirmatn.variable}>
      <body className="font-sans antialiased">
        <div className="min-h-screen">
          {/* Header placeholder — Phase 3 builds full navigation */}
          <header className="border-b border-gray-200 px-4 py-3">
            <div className="mx-auto max-w-7xl">
              <h1 className="text-xl font-bold">کیدتیوب</h1>
            </div>
          </header>
          <main className="mx-auto max-w-7xl px-4 py-6">
            {children}
          </main>
        </div>
      </body>
    </html>
  );
}
```

Key RTL requirements:
- `<html lang="fa" dir="rtl">` — satisfies RTL-01
- Font loaded via `next/font` — satisfies RTL-04 (zero layout shift via CSS variable)
- All layout uses logical properties (no `margin-left`, use `ms-*` / `me-*` / `ps-*` / `pe-*` in Tailwind)

**4. Update `site-app/tailwind.config.ts`:**
```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-vazirmatn)', 'Vazirmatn', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
export default config;
```

**5. Update `site-app/src/app/globals.css`:**
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Ensure logical properties are used throughout */
/* Tailwind v3.3+ supports logical properties via ms-*, me-*, ps-*, pe-* utilities */
```

**6. Update `site-app/src/app/page.tsx`** — simple placeholder:
```tsx
export default function Home() {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">صفحه اصلی</h2>
      <p className="text-gray-600">به کیدتیوب خوش آمدید</p>
    </div>
  );
}
```

**7. Update `site-app/next.config.ts`:**
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  output: 'standalone',
};

export default nextConfig;
```
The `standalone` output mode is required for Docker deployment (creates a self-contained build).

**8. Create `site-app/Dockerfile`:**
```dockerfile
FROM node:20-alpine AS base

FROM base AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

FROM base AS runner
WORKDIR /app
ENV NODE_ENV=production
RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
USER nextjs
EXPOSE 3000
ENV PORT=3000
CMD ["node", "server.js"]
```

Verify: `cd site-app && npm run build` completes without errors.
  </action>
  <verify>
    <automated>cd /Users/hadi/Documents/Self/kidtube/site-app && npm run build 2>&1 | tail -5; echo "exit: $?"</automated>
  </verify>
  <done>site-app builds without errors, root layout has dir="rtl" and lang="fa", Vazirmatn font configured via next/font with 4 weights, Tailwind CSS configured with Vazirmatn as default sans font, standalone output mode enabled, Dockerfile created</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold admin-app with Next.js 15, RTL layout, and Vazirmatn font</name>
  <files>admin-app/package.json, admin-app/tsconfig.json, admin-app/next.config.ts, admin-app/tailwind.config.ts, admin-app/postcss.config.mjs, admin-app/src/app/globals.css, admin-app/src/app/layout.tsx, admin-app/src/app/page.tsx, admin-app/src/lib/fonts.ts, admin-app/Dockerfile</files>
  <action>
Create `admin-app/` — same approach as site-app:
```bash
cd /Users/hadi/Documents/Self/kidtube && npx create-next-app@latest admin-app --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm
```

Install Vazirmatn:
```bash
cd admin-app && npm install vazirmatn
```

**Per CONTEXT.md decision:** Admin-app also in Persian RTL (same font and direction setup as site-app).

Create the same file set as Task 1 with these differences:

**`admin-app/src/lib/fonts.ts`** — identical to site-app version.

**`admin-app/src/app/layout.tsx`:**
```tsx
import type { Metadata } from 'next';
import { vazirmatn } from '@/lib/fonts';
import './globals.css';

export const metadata: Metadata = {
  title: 'کیدتیوب — مدیریت',
  description: 'پنل مدیریت کیدتیوب',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="fa" dir="rtl" className={vazirmatn.variable}>
      <body className="font-sans antialiased">
        <div className="min-h-screen">
          <header className="border-b border-gray-200 bg-gray-50 px-4 py-3">
            <div className="mx-auto max-w-7xl">
              <h1 className="text-xl font-bold">پنل مدیریت کیدتیوب</h1>
            </div>
          </header>
          <main className="mx-auto max-w-7xl px-4 py-6">
            {children}
          </main>
        </div>
      </body>
    </html>
  );
}
```

**`admin-app/src/app/page.tsx`:**
```tsx
export default function AdminHome() {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">داشبورد مدیریت</h2>
      <p className="text-gray-600">پنل مدیریت کیدتیوب</p>
    </div>
  );
}
```

**`admin-app/next.config.ts`:**
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  output: 'standalone',
  basePath: '/admin',
};

export default nextConfig;
```
The `basePath: '/admin'` ensures all admin-app routes are prefixed with `/admin`, matching the nginx routing in Plan 01 where `/admin/` proxies to admin-app.

**`admin-app/tailwind.config.ts`** — identical to site-app version.

**`admin-app/Dockerfile`:**
Same as site-app Dockerfile but expose port 3001 and set `ENV PORT=3001`.

Verify: `cd admin-app && npm run build` completes without errors.
  </action>
  <verify>
    <automated>cd /Users/hadi/Documents/Self/kidtube/admin-app && npm run build 2>&1 | tail -5; echo "exit: $?"</automated>
  </verify>
  <done>admin-app builds without errors, root layout has dir="rtl" and lang="fa", Vazirmatn font configured identically to site-app, basePath set to "/admin" for nginx routing, standalone output mode enabled, Dockerfile created</done>
</task>

</tasks>

<verification>
1. `cd site-app && npm run build` — exits 0
2. `cd admin-app && npm run build` — exits 0
3. Both layout.tsx files contain `<html lang="fa" dir="rtl"`
4. Both apps have Vazirmatn loaded via next/font with weights 300, 400, 500, 700
5. No `margin-left`, `margin-right`, `padding-left`, `padding-right` in any component (only logical properties)
6. admin-app has `basePath: '/admin'` in next.config.ts
7. Both Dockerfiles use multi-stage build with standalone output
</verification>

<success_criteria>
- Both Next.js apps build without errors
- Root html elements have dir="rtl" and lang="fa" attributes (RTL-01)
- Vazirmatn font loaded via next/font with zero layout shift (RTL-04)
- Tailwind CSS configured with logical properties support (RTL-02)
- All layout utilities use CSS logical properties (ms-*, me-*, ps-*, pe-*)
- Dockerfiles produce standalone deployable images
- admin-app basePath is /admin for correct nginx routing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md`
</output>
