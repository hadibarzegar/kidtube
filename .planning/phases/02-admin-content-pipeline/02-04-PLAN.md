---
phase: 02-admin-content-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - admin-app/src/app/layout.tsx
  - admin-app/src/app/page.tsx
  - admin-app/src/app/globals.css
  - admin-app/src/components/Sidebar.tsx
  - admin-app/src/components/DataTable.tsx
  - admin-app/src/lib/api.ts
  - admin-app/src/lib/session.ts
  - admin-app/src/middleware.ts
  - admin-app/src/app/login/page.tsx
  - admin-app/src/app/actions/auth.ts
  - admin-app/src/app/categories/page.tsx
  - admin-app/src/app/categories/[id]/page.tsx
  - admin-app/src/app/age-groups/page.tsx
  - admin-app/src/app/age-groups/[id]/page.tsx
  - admin-app/next.config.ts
autonomous: true
requirements: [CONT-03, CONT-04, CONT-06, ADMN-01]

must_haves:
  truths:
    - "Admin can log in via a login form and is redirected to the dashboard"
    - "Unauthenticated users are redirected to the login page"
    - "Admin panel has a fixed left sidebar with nav links: Channels, Episodes, Categories, Age Groups, Jobs"
    - "Admin panel UI is in English with LTR layout"
    - "Admin can view, create, edit, and delete categories through a data table and form pages"
    - "Admin can view, create, edit, and delete age groups through a data table and form pages"
    - "Data tables have sortable columns and a search bar"
  artifacts:
    - path: "admin-app/src/components/Sidebar.tsx"
      provides: "Fixed left sidebar with navigation links"
      min_lines: 20
    - path: "admin-app/src/components/DataTable.tsx"
      provides: "Reusable data table with sort and search"
      min_lines: 40
    - path: "admin-app/src/lib/api.ts"
      provides: "Fetch wrapper for /api/admin/* calls"
      exports: ["apiFetch"]
    - path: "admin-app/src/lib/session.ts"
      provides: "Cookie-based JWT session management"
      exports: ["createSession", "getSession", "deleteSession"]
    - path: "admin-app/src/middleware.ts"
      provides: "Route protection redirecting to login"
      contains: "admin_token"
    - path: "admin-app/src/app/login/page.tsx"
      provides: "Login form page"
      min_lines: 15
    - path: "admin-app/src/app/categories/page.tsx"
      provides: "Categories list with DataTable"
      min_lines: 20
    - path: "admin-app/src/app/age-groups/page.tsx"
      provides: "Age groups list with DataTable"
      min_lines: 20
  key_links:
    - from: "admin-app/src/middleware.ts"
      to: "admin-app/src/app/login/page.tsx"
      via: "Redirects unauthenticated users to /admin/login"
      pattern: "redirect.*login"
    - from: "admin-app/src/app/actions/auth.ts"
      to: "admin-app/src/lib/session.ts"
      via: "createSession stores JWT in httpOnly cookie after login"
      pattern: "createSession"
    - from: "admin-app/src/app/categories/page.tsx"
      to: "admin-app/src/lib/api.ts"
      via: "Fetches categories from /api/admin/categories"
      pattern: "apiFetch.*categories"
---

<objective>
Admin dashboard UI shell (layout, sidebar, login, route protection) and simple CRUD pages for categories and age groups.

Purpose: Establishes the admin app foundation — sidebar navigation, authentication flow, reusable DataTable component, and API fetch helper. Categories and age groups are the simplest CRUD entities, so they prove the UI pattern that channels, episodes, and jobs pages will follow in Plan 05.

Output: Working admin login flow, sidebar layout, protected routes, reusable DataTable component, and full CRUD UI for categories and age groups.
</objective>

<execution_context>
@/Users/hadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-content-pipeline/02-RESEARCH.md
@.planning/phases/02-admin-content-pipeline/02-CONTEXT.md
@.planning/phases/02-admin-content-pipeline/02-01-SUMMARY.md
@.planning/phases/02-admin-content-pipeline/02-02-SUMMARY.md

Source files (read before implementing):
@admin-app/src/app/layout.tsx
@admin-app/src/app/page.tsx
@admin-app/src/app/globals.css
@admin-app/src/lib/fonts.ts
@admin-app/next.config.ts

<interfaces>
<!-- Admin API endpoints created in Plan 02 -->

POST /api/admin/auth/login — body: {"email", "password"} → {"token": "jwt..."}
GET  /api/admin/categories — → [{"id", "name", "created_at", "updated_at"}]
POST /api/admin/categories — body: {"name"} → 201 + category
GET  /api/admin/categories/{id} → category object
PUT  /api/admin/categories/{id} — body: {"name"} → updated category
DELETE /api/admin/categories/{id} → 204

GET  /api/admin/age-groups — → [{"id", "name", "min_age", "max_age", ...}]
POST /api/admin/age-groups — body: {"name", "min_age", "max_age"} → 201
GET  /api/admin/age-groups/{id} → age group object
PUT  /api/admin/age-groups/{id} → updated
DELETE /api/admin/age-groups/{id} → 204

<!-- Internal Docker DNS for server-side calls -->
Server actions call: http://admin-api:8082/... (via ADMIN_API_INTERNAL_URL env var)
Client-side fetch calls: /api/admin/... (via nginx proxy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session, API helper, middleware, and login page</name>
  <files>
    admin-app/src/lib/session.ts
    admin-app/src/lib/api.ts
    admin-app/src/middleware.ts
    admin-app/src/app/login/page.tsx
    admin-app/src/app/actions/auth.ts
    admin-app/next.config.ts
  </files>
  <action>
    1. Install jose: `cd /Users/hadi/Documents/Self/kidtube/admin-app && npm install jose`

    2. Create `admin-app/src/lib/session.ts`:
       ```typescript
       import 'server-only'
       import { cookies } from 'next/headers'

       export async function createSession(token: string) {
         const cookieStore = await cookies()
         cookieStore.set('admin_token', token, {
           httpOnly: true,
           secure: process.env.NODE_ENV === 'production',
           sameSite: 'lax',
           maxAge: 60 * 60 * 24 * 7, // 7 days
           path: '/',  // CRITICAL: must be '/' not '/admin' — cookie must be sent to /api/admin/*
         })
       }

       export async function getSession(): Promise<string | null> {
         const cookieStore = await cookies()
         return cookieStore.get('admin_token')?.value ?? null
       }

       export async function deleteSession() {
         const cookieStore = await cookies()
         cookieStore.delete('admin_token')
       }
       ```

    3. Create `admin-app/src/lib/api.ts`:
       - Export `apiFetch(path: string, options?: RequestInit): Promise<Response>` for client-side use
       - Prepends `/api/admin` to path, sets `credentials: 'include'`
       - Export `apiServerFetch(path: string, options?: RequestInit): Promise<Response>` for server actions
       - Uses `process.env.ADMIN_API_INTERNAL_URL` (Docker internal DNS) — falls back to `http://localhost:8082` for local dev
       - Both functions add `Content-Type: application/json` header by default

    4. Create `admin-app/src/middleware.ts`:
       - Protect all routes except `/admin/login`
       - Check for `admin_token` cookie — redirect to `/admin/login` if missing
       - If on login page with valid cookie, redirect to `/admin/channels`
       - Matcher: `['/admin/:path*']`
       - IMPORTANT: basePath is '/admin', so Next.js routes are relative to that. The middleware matcher pattern should account for this. Test both with and without basePath prefix.

    5. Create `admin-app/src/app/actions/auth.ts`:
       - `'use server'` directive
       - `export async function login(prevState: unknown, formData: FormData)` — server action for login form
       - Extract email and password from formData
       - Fetch `${process.env.ADMIN_API_INTERNAL_URL}/auth/login` with POST
       - On success: call `createSession(token)` then `redirect('/admin/channels')`
       - On failure: return `{ error: 'Invalid credentials' }`
       - `export async function logout()` — deletes session and redirects to login

    6. Create `admin-app/src/app/login/page.tsx`:
       - Client component ('use client')
       - Use `useActionState(login, undefined)` from React 19
       - Simple centered login form: email input, password input, submit button
       - Show error message from state if present
       - Show "Signing in..." on pending state
       - Style with Tailwind: centered card, clean form layout
       - This page is in English (admin panel is English per locked decision)
       - LTR layout on this page (the root layout will be updated in Task 2)

    7. Update `admin-app/next.config.ts` to add `serverExternalPackages` if needed for jose, and ensure `ADMIN_API_INTERNAL_URL` is exposed to server components via `env` config if Next.js requires it.
  </action>
  <verify>
    cd /Users/hadi/Documents/Self/kidtube/admin-app && npx next build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"
  </verify>
  <done>
    Login page renders with email/password form; server action calls admin-api /auth/login and stores JWT in httpOnly cookie; middleware redirects unauthenticated users to login; api.ts provides fetch wrappers for client-side and server-side calls; admin-app builds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create layout with sidebar and LTR admin shell</name>
  <files>
    admin-app/src/app/layout.tsx
    admin-app/src/app/page.tsx
    admin-app/src/app/globals.css
    admin-app/src/components/Sidebar.tsx
    admin-app/src/components/DataTable.tsx
  </files>
  <action>
    1. Create `admin-app/src/components/Sidebar.tsx`:
       - Server component (no 'use client' needed for static nav)
       - Fixed left sidebar, full viewport height, w-64 (16rem)
       - Background: dark gray or slate (e.g., bg-slate-800 text-white)
       - Logo/title area at top: "KidTube Admin"
       - Nav links using Next.js `<Link>`:
         - Channels → /admin/channels
         - Episodes → /admin/episodes
         - Categories → /admin/categories
         - Age Groups → /admin/age-groups
         - Jobs → /admin/jobs
       - Active link highlighting (use `usePathname()` from 'next/navigation' — this means it DOES need 'use client')
       - Logout button at bottom of sidebar (calls logout server action via form)
       - All text in English per locked decision

    2. Create `admin-app/src/components/DataTable.tsx`:
       - Client component ('use client') — needs state for search/sort
       - Generic reusable table component
       - Props: `columns: {key: string, label: string, sortable?: boolean}[]`, `data: any[]`, `onDelete?: (id: string) => void`, `editPath?: string`
       - Features:
         - Search bar at top that filters rows by matching any column value (case-insensitive)
         - Sortable column headers (click to toggle asc/desc) — indicated by arrow icon
         - Each row has Edit link (navigates to `{editPath}/{id}`) and Delete button (calls onDelete)
         - Tailwind styling: full-width table, striped rows, hover highlight, rounded corners
       - Return empty state message when no data matches search

    3. Update `admin-app/src/app/layout.tsx`:
       - Change `dir="rtl"` to `dir="ltr"` and `lang="fa"` to `lang="en"` — admin panel is English LTR per locked decision
       - Keep Vazirmatn font variable class on html (still used for any Persian content preview)
       - Layout structure: flex row with Sidebar on left, main content area on right
       - Main content: `flex-1 min-h-screen bg-gray-50 p-6`
       - Conditionally show sidebar (not shown on login page). Use a layout group or check pathname.
         - **Approach:** Create `admin-app/src/app/(dashboard)/layout.tsx` for sidebar-wrapped pages, OR handle it in the root layout by checking if current path is login.
         - **Simpler approach:** Keep sidebar in root layout. The middleware already redirects unauthenticated users to login, so the sidebar is only visible to logged-in users anyway. On the login page, the sidebar will briefly flash before redirect — to avoid this, use conditional rendering based on pathname or use route groups.
         - **Best approach:** Use route groups. Move all authenticated pages under `(dashboard)` group with its own layout that includes Sidebar. Login page stays at root level.
         - Actually, for simplicity: put Sidebar in root layout but only render it when NOT on login page. Use a client wrapper that checks pathname.

    4. Update `admin-app/src/app/page.tsx`:
       - Redirect to `/admin/channels` using `redirect()` from next/navigation (server-side redirect)

    5. Update `admin-app/src/app/globals.css`:
       - Keep Tailwind import and @theme
       - This should remain as-is — Tailwind v4 handles everything

    6. Style the DataTable with Tailwind: clean data-dense layout suitable for admin dashboards. No rounded cards or excessive whitespace — prioritize information density.
  </action>
  <verify>
    cd /Users/hadi/Documents/Self/kidtube/admin-app && npx next build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"
  </verify>
  <done>
    Root layout is LTR English with sidebar navigation; Sidebar has 5 nav items (Channels, Episodes, Categories, Age Groups, Jobs) with active state; DataTable component supports search, sort, edit link, and delete button; root page redirects to /admin/channels; admin-app builds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Categories and Age Groups CRUD pages</name>
  <files>
    admin-app/src/app/categories/page.tsx
    admin-app/src/app/categories/[id]/page.tsx
    admin-app/src/app/age-groups/page.tsx
    admin-app/src/app/age-groups/[id]/page.tsx
  </files>
  <action>
    **Categories list page (`categories/page.tsx`):**
    - Server component — fetch categories from admin-api via server-side fetch using `apiServerFetch('/categories')`
    - Pass the `admin_token` cookie in the fetch request header for authentication
    - Render DataTable with columns: Name, Created At
    - "New Category" button linking to `/admin/categories/new`
    - Delete action: call server action that DELETEs `/categories/{id}` then `revalidatePath('/admin/categories')`

    **Categories form page (`categories/[id]/page.tsx`):**
    - Server component with client form
    - If `id === 'new'`: show empty create form
    - Otherwise: fetch category by ID, pre-fill form for editing
    - Form fields: Name (text input, required)
    - Submit via server action: POST (create) or PUT (update) to `/categories` or `/categories/{id}`
    - On success: `redirect('/admin/categories')`
    - On error: show error message
    - "Back to Categories" link at top
    - Dedicated full-page form per locked decision (not modal/inline)

    **Age Groups list page (`age-groups/page.tsx`):**
    - Same pattern as categories
    - DataTable columns: Name, Min Age, Max Age, Created At
    - "New Age Group" button

    **Age Groups form page (`age-groups/[id]/page.tsx`):**
    - Same pattern as categories form
    - Form fields: Name (text, required), Min Age (number, required, min=0), Max Age (number, required)
    - Client-side validation: max age must be greater than min age

    **Server actions for CRUD:**
    - Create `admin-app/src/app/actions/categories.ts` and `admin-app/src/app/actions/age-groups.ts`
    - Each has: create, update, delete server actions
    - All use `apiServerFetch` with the admin_token cookie forwarded
    - Forwarding cookie in server actions: read with `cookies().get('admin_token')` and pass as `Authorization: Bearer {token}` header or `Cookie: admin_token={token}` header to admin-api

    **Important patterns:**
    - All fetch calls from server to admin-api use Docker internal DNS (ADMIN_API_INTERNAL_URL)
    - The Go backend expects JWT either from cookie or Authorization header. Since server actions make server-to-server calls (not browser requests), pass the token as `Authorization: Bearer {token}` header.
    - Use `revalidatePath` after mutations to refresh the list page data
  </action>
  <verify>
    cd /Users/hadi/Documents/Self/kidtube/admin-app && npx next build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"
  </verify>
  <done>
    Categories page shows data table with search/sort and CRUD operations; Age Groups page shows data table with min/max age columns and CRUD; both have dedicated full-page create/edit forms; all mutations use server actions with proper auth forwarding; admin-app builds without errors.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/hadi/Documents/Self/kidtube/admin-app && npx next build` — builds without errors
2. `grep -q "admin_token" admin-app/src/middleware.ts` — middleware checks for JWT cookie
3. `grep -q "Sidebar" admin-app/src/app/layout.tsx` — sidebar rendered in layout
4. `grep -rq "DataTable" admin-app/src/app/categories/page.tsx` — categories uses DataTable
5. `ls admin-app/src/app/categories/page.tsx admin-app/src/app/categories/\\[id\\]/page.tsx admin-app/src/app/age-groups/page.tsx admin-app/src/app/age-groups/\\[id\\]/page.tsx` — all pages exist
</verification>

<success_criteria>
- Admin can log in and see sidebar navigation
- Unauthenticated users are redirected to login
- Admin panel is in English with LTR layout
- DataTable component supports search and sortable columns
- Categories: full CRUD with data table + form pages
- Age Groups: full CRUD with data table + form pages, min/max validation
- All pages navigate via sidebar links
- admin-app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-content-pipeline/02-04-SUMMARY.md`
</output>
